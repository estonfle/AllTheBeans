The Project Structure

welcom-offer/
├── orval.config.ts             # Configures Orval to use mutator.ts and output to types/generated/
├── vite.config.ts              # Vite config (includes unplugin-vue-router setup)
├── vitest.config.ts            # Test runner configuration
├── package.json                # Scripts including generate-types pipeline
│
└── src/
    ├── types/                  # Definition files
    │   └── env.d.ts         
    ├── main.ts                 # App entry point (initializes Vue, Pinia, i18n, Vue Query)
    ├── App.vue                 # Root component (contains Layout wrapper and <RouterView />)
    │
    ├── app/                    # Global Configuration & Bootstrapping
    │   ├── i18n/               
    │   │   ├── locales/        
    │   │   │   └── en/         # English language namespaces
    │   │   │       ├── common.json      # Shared global strings (buttons, general errors)
    │   │   │       ├── offers.json      # Welcome offer specific translations
    │   │   │       └── promotions.json  # Promotions feature translations
    │   │   └── config.ts       # vue-i18n instance initialization & namespace loading
    │   │
    │   ├── router/             
    │   │   └── guards.ts       # Global navigation guards (e.g., Okta auth checks)
    │   │
    │   └── queryClient.ts      # Vue Query global config (staleTime, cacheTime)
    │
    ├── api/                    # HTTP Layer & Client Setup
    │   ├── axios-client.ts     # Custom Axios instance (baseURL setup, Okta interceptors)
    │   └── mutator.ts          # Custom Axios fetcher implementation for Orval to use
    │
    ├── assets/                 # Global Styles & Static Assets
    │   ├── styles/             # Global stylesheets
    │   │   ├── bootstrap_custom.scss # Custom Bootstrap 5 variable overrides (colors, spacing)
    │   │   ├── styles.scss     # Main application styles entry point
    │   │   └── variables.scss  # Shared CSS/SCSS variables
    │   ├── flags/              # Country flags 
    │   ├── fonts/              # Custom typography and font files
    │   └── logos/              # App branding assets
    │
    ├── components/             # GLOBAL Reusable UI (Agnostic to business logic, Atomic Design concept)
    │   ├── base/               # The "Dumb" Atoms
    │   │   ├── BaseButton.vue  # Bootstrap <button class="btn...">
    │   │   └── BaseInput.vue   # Bootstrap <input>
    │   ├── forms/              # Reusable Molecules
    │   │   └── BaseFormGroup.vue # Wrapper combining label, BaseInput, and VeeValidate errors
    │   ├── complex/            # Reusable Organisms
    │   │   ├── BaseDataTable.vue # Generic table handling pagination/sorting
    │   │   ├── BaseModal.vue   # Bootstrap Modal wrapper
    │   │   └── BaseSearch.vue  # Reusable search input with debouncing and dropdown
    │   └── layout/             # Structural application components
    │       ├── AppHeader.vue   # Top navigation bar
    │       ├── AppSidebar.vue  # Collapsible side navigation
    │       └── ErrorBoundary.vue # Global component error catcher
    │
    ├── layouts/                # Structural Page Wrappers
    │   ├── DashboardLayout.vue    # Main authenticated layout (Header + Sidebar)
    │   └── WelcomeOfferLayout.vue # Specific layout for the welcome offer flows
    │
    ├── pages/                  # 🪄 AUTO-MAPPED BY unplugin-vue-router
    │   ├── index.vue           # Maps to "/"
    │   ├── login.vue           # Maps to "/login"
    │   ├── callback.vue        # Maps to "/callback" (Okta OIDC redirect handler)
    │   └── promotions/
    │       └── index.vue       # Maps to "/promotions"
    │       └── [id].vue        # Maps to "/promotions/:id" (Promotion detail page)
    │
    ├── stores/                 # GLOBAL Client State (Pinia)
    │   ├── authStore.ts        # Manages Okta OIDC flow, auth state, active JWT
    │   ├── sessionStore.ts     # Manages sessions
    │   ├── uiStore.ts          # Global UI state (sidebar collapsed, theme preference)
    │   └── userStore.ts        # Manages active user details, roles, and preferences
    │
    ├── types/                  # Definition files
    │   └── generated/          # ⚠️ 100% AUTO-GENERATED BY ORVAL (Do not edit manually)
    │       ├── endpoints/      
    │       │   └── promotions/
    │       │       ├── promotions.msw.ts # MSW mocks specific to the promotions endpoints
    │       │       └── promotions.ts     # Vue Query composables (e.g., useGetPromotions)
    │       └── models/         
    │           ├── promotions.ts         # TypeScript models from .NET DTOs
    │           └── welcomeOffer.ts       # TypeScript models from .NET DTOs
    │
    ├── features/               # 🚀 DOMAIN MODULES (The core of enterprise scale)
    │   ├── promotions/
    │   │   ├── components/     # Specific to promotions (e.g., PromotionCard.vue)
    │   │   └── composables/    # Domain logic combining stores/queries
    │   └── welcome-offer/
    │       └── components/     # Specific to welcome offers
    │
    ├── utils/                  # Global Utility Functions
    │   ├── date.ts             # Date formatting helpers
    │   └── validation.ts       # Shared Zod regex patterns
    │
    └── test/                   # Testing infrastructure
        ├── setup.ts            # Vitest global setup (mounts i18n, Pinia for tests)
        └── msw/
            ├── browser.ts      # MSW setup for local dev
            └── server.ts       # MSW setup for Vitest (imports Orval's generated mocks)

Architectural Guidelines for the Stack
A. Component Layering (The "Russian Doll" approach)
Since we want generic components, strict discipline is required to keep them reusable:
Base Components (components/base): Should be incredibly "dumb". They emit events and take props. They should never make API calls or access Pinia stores.
Form Wrappers (components/forms): Combine a BaseInput with a label, validation error text, and hint text.
Feature Components (features/*/components): These are "smart". A UserSearchDropdown.vue will import a BaseSelect, but it will internally use the Orval-generated Vue Query to fetch users and map them to the dropdown.
B. State Management Split
Do not duplicate state.
Vue Query (Server State): All API data fetching, caching, loading states, and data synchronization should be handled by the composables generated by Orval.
Pinia (Client State): Only use Pinia for things the server doesn't care about (e.g., isSidebarOpen, currentUserToken, themeMode, or complex temporary state like a 5-step checkout wizard).
C. API & Orval Configuration
Configure Orval to generate Vue Query composables directly.
Tell Orval to use custom src/api/axios.ts instance.
Tell Orval to output MSW handlers. This means when the .NET backend adds a field to an endpoint, Orval updates types, queries, and mock data simultaneously.
D. Error Handling Strategy
Enterprise error handling needs a tiered approach:
Axios Interceptors: Catch global network errors. If 401, trigger a token refresh or redirect to login. If 500, trigger a global toast notification ("System error occurred").
Vue Query Global Cache: In queryClient.ts, we can set a global onError handler for mutations to automatically show a toast notification with the backend's validation error messages.
Form Level: Map backend 400 Bad Request validation errors directly to form components.
Vue Error Handler: Use app.config.errorHandler to catch generic JavaScript runtime exceptions and log them to a service like Sentry or Application Insights.
E. Testing (Vitest + MSW)
By keeping tests colocated or strictly separated, we maintain sanity.
Unit tests for logic/utils (formatDate.test.ts).
Component tests for Base components (BaseButton.test.ts).
Integration tests for Feature Views. Mount the view, let Vue Query trigger the Axios call, which is intercepted by the Orval-generated MSW mock. Assert that the DOM updates.

Vue Router vs. unplugin-vue-router
Traditional vue-router: Manually maintain a router/index.ts file, importing every component and defining the paths ({ path: '/users/:id', component: UserDetail }).
Pros: Explicit, standard, easy to see the whole map in one place.
Cons: As the enterprise app grows to 100+ routes, this file becomes a massive bottleneck and merge-conflict hotspot.
unplugin-vue-router (File-Based Routing): It uses vue-router under the hood, but it auto-generates the routes based on file/folder structure (just like Next.js or Nuxt). If we create src/pages/users/[id].vue, the route /users/:id is automatically created.
Pros: Strict TypeScript support. It generates types for routes, meaning <router-link to="/usrs"> will throw a TypeScript error because the route is misspelled. It also auto-handles code-splitting.
Cons: We have to learn the file naming conventions.

Key Synergy Points Highlighted in this Structure:
pages/ vs features/:
The pages/ directory stays incredibly thin. For example, pages/users/index.vue will simply import and render <UserList /> from features/users/components/.
This keeps routing logic decoupled from complex business logic.
The "Holy Trinity" of Forms (BaseInput.vue, UserForm.vue, zodErrorMap.ts):
BaseInput.vue just asks VeeValidate: "Do I have an error?" and applies Bootstrap's is-invalid.
UserForm.vue defines the Zod schema and passes the Orval mutation on submit.
zodErrorMap.ts intercepts Zod and injects the proper vue-i18n language string seamlessly.
The generated/ folder:
It sits inside types/ and is strictly hands-off. When the .NET API is updated, simpley run the "generate-types" script, and the Models, Vue Query hooks, and Vitest MSW mocks are all updated instantly.

Authentication Flow (authStore.ts & interceptors.ts & pages/callback.vue):
Okta directs the user to pages/callback.vue.
The callback page tells authStore.ts to validate and store the JWT.
api/interceptors.ts reads the JWT from Pinia and attaches it to every Axios request.

The mutator.ts pattern: By placing generated code into types/ and keeping mutator.ts and axios-client.ts in api/, we completely separate custom HTTP networking logic from the auto-generated code. Orval will simply inject mutator.ts into the Vue Query composables in types/generated/.

BaseFormGroup.vue: Wrapping input logic here reduces code duplication immensely. Instead of placing labels and validation messages in every form, BaseFormGroup cleanly encapsulates this.

Namespaced i18n: Having folders for en/ with files like promotions.json allows us to load translations specifically when a user visits the Promotions page, which saves initial bundle size in massive apps.