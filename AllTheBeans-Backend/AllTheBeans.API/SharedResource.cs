namespace AllTheBeans.API;

// This class doesn't do anything. It just acts as an anchor for the localizer.
public class SharedResource
{
    // The constant name and value will perfectly match the key in the .resx file
    public const string WelcomeMessage = nameof(WelcomeMessage);
    public const string InvalidEmailError = nameof(InvalidEmailError);
}